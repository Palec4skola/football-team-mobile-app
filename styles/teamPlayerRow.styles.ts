import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  playerItem: {
    paddingVertical: 10,
    borderBottomWidth: 1,
    borderColor: '#ccc',
  },
  playerText: {
    fontSize: 16,
  },
});
